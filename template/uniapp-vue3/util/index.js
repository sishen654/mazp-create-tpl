export { to, redirect, preload, back } from "./route"
export { setStorage, getStorage, deleteStorage, inStorage } from "./storage"
export { axios } from "./request"

export function test() {
  console.log("test");
}
